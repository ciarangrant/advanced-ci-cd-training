# CI/CD Pipeline — Hackathon (Local Labs)
# TODO: Complete the placeholder steps marked with TODO comments

name: Deploy Pipeline
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
      - uses: actions/setup-java@c5195efecf7bdfc987ee8bae7a71cb8b11521c00
        with:
          java-version: 21
          distribution: temurin
          cache: maven
      - run: ./mvnw test

  build-and-push:
    needs: test
    if: github.event_name == 'push'
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
      # TODO: Log in to the Gitea container registry
      # Use docker/login-action with:
      #   registry: localhost:8100
      #   username: ${{ secrets.GITEA_USERNAME }}
      #   password: ${{ secrets.GITEA_TOKEN }}
      # Pin the action to a commit SHA

      # TODO: Build and push the Docker image
      # Use docker/build-push-action with:
      #   push: true
      #   tags: localhost:8100/${{ gitea.repository }}:${{ gitea.sha }}
      # Pin the action to a commit SHA

  terraform-plan:
    needs: test
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
      - uses: hashicorp/setup-terraform@b9cd54a3c349d3f38e8881555e7e3f22b67044a7
        with:
          terraform_version: 1.14.8
      # TODO: Run terraform init and terraform plan
      # Set working-directory to ./terraform-local
      # Pass image_tag and gitea_user as variables: -var="image_tag=${{ gitea.sha }}" -var="gitea_user=${{ gitea.actor }}"

  terraform-apply:
    needs: build-and-push
    if: github.event_name == 'push'
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
      - uses: hashicorp/setup-terraform@b9cd54a3c349d3f38e8881555e7e3f22b67044a7
        with:
          terraform_version: 1.14.8
      # TODO: Run terraform init and terraform apply
      # Set working-directory to ./terraform-local
      # Use -auto-approve flag
      # Pass image_tag, gitea_user, and blue/green weights as variables

  verify:
    needs: terraform-apply
    runs-on: ubuntu-24.04
    steps:
      # TODO: Add a health check with retry logic
      # Wait for deployment to register, then poll the health endpoint
      # Use http://java-web.local/actuator/health as the URL
      # Retry up to 10 times with 10-second intervals
      # Exit 0 on HTTP 200, exit 1 after all retries fail
      - name: Placeholder
        run: echo "TODO - implement health check"
